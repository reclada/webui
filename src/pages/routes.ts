export const routes = {
  root: '/',
  files: '/files',
  datasets: '/datasets',
  datasources: '/datasources',
  assets: '/assets',
  available: '/available',
  search: '/search',
  authCallback: '/callback',
};
